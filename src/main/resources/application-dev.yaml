spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/${DB_NAME}
    username: postgres
    password: root

server:
  port: ${SERVER_PORT}

currency:
  converter:
    service:
      url: http://localhost:8083

ngrok:
  tunnel-url: http://localhost:${server.port}